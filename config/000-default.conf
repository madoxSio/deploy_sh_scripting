<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/html

    <Directory /var/www/html>
        AuthType Basic 
        AuthName "Authentification requises pour accéder au serveur apache hsl"
        AuthUserFile /etc/apche2/.htpasswd
        <RequireAll>
            Require valid-user
        </RequireAll>
    </Directory>

</VirtualHost>