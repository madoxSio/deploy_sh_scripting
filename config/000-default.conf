<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/html

    <Directory /var/www/html>
        AuthType Basic 
        AuthName "Authentification requises pour accéder au serveur apache hsl"
        AuhtUserFile /etc/apche2/.htpasswd
        Require valid-user
    </Directory>

</VirtualHost>